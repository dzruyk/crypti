1) sync stream after errors
3) array memory management
4) function memory management
5) library funtions(can user redefine reserved names for lib funcs?)
8) pay attention for blocks of insturctions
9) libbignum integration
10) нужно придумать как при traverse копировать function body
    без дополнительных расходов и костылей(сейчас func-> body не убивает деструктор)
11) функции и переменные не должны иметь одного имени
12) function errors context when traverse body
15) need to travese statesment list in functions
16) check when assign res of function call to value(can be void)
19) unused lex item field(op)??
20) ast_node_return_t
21) pass argues to funtion call by value, not by copy
22) traverse_ctx( sad but need to dynamically allocate)
23) may be need to get rel_op() between LPAR and RPAR
24) stack of lex items(need to implement tok_prev()
25) implement STACK_POP_OR_WARN wrapper (need to reduce overhead in traverse.c)
26) implement copy-on-write val inside new scope + global modificator
27) variable lenght argues functions(builtin and user defined)
